apiVersion: v1
kind: Pod
metadata:
  name: multi-container
  labels:
    project: roboshop
    purpose: Multi-container-demo
spec:
    containers:
      - name: nginx
        image: nginx
      - name: almalinux:9
        image: nginx
        command: ["sleep", "1000"]  
